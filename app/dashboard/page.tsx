'use client';
import Link from 'next/link'; import {useState} from 'react';
const nav=['Dashboard','Profile','My Applications','My Subscription','Payments','Documents','Vehicle','Service & Maintenance','Charging','End of Subscription'];
export default function Dashboard(){const [active,setActive]=useState('Dashboard');return <><header className="header"><div className="container nav"><Link className="logo" href="/"> <span className="logo-mark">S</span>SUZUKI DRIVE</Link><span className="pill">Customer Dashboard</span></div></header><div className="dash-shell"><aside className="sidebar">{nav.map(x=><button className={`side-link ${active===x?'active':''}`} style={{border:0,textAlign:'left'}} key={x} onClick={()=>setActive(x)}>{x}</button>)}</aside><main className="dash-main"><div style={{display:'flex',justifyContent:'space-between',gap:20,alignItems:'start'}}><div><div className="eyebrow">Suzuki Drive</div><h1>{active}</h1><p className="muted">Your EV journey, all in one place.</p></div><span className="status">Active</span></div>{active==='Dashboard'&&<><div className="grid dash-cards" style={{margin:'26px 0'}}>{[['My Subscription','36 months'],['Payments','Next payment ₹16,100'],['Charging','Credits & recent activity'],['Vehicle','e-Discover']].map(([h,v])=><div className="card dash-card" key={h}><div className="muted" style={{fontSize:12}}>{h}</div><strong style={{display:'block',marginTop:8}}>{v}</strong></div>)}</div><div className="vehicle-banner"><div className="eyebrow">Your vehicle</div><h2>e-Discover</h2><p className="muted">30 kWh · 275 km · 1,200 km included/month</p><div style={{display:'flex',gap:10,flexWrap:'wrap'}}><button className="btn btn-primary" onClick={()=>setActive('Vehicle')}>View vehicle</button><button className="btn btn-light" onClick={()=>setActive('Charging')}>Find a charger</button></div></div></>}{active==='End of Subscription'&&<div className="grid action-grid"><div className="card form-card"><div className="eyebrow">Option 01</div><h2>Return your EV</h2><p className="muted">Start the return process and arrange vehicle pickup at the end of your subscription.</p><button className="btn btn-light">Start return</button></div><div className="card form-card"><div className="eyebrow">Option 02</div><h2>Buy your EV</h2><p className="muted">Buy the exact leased vehicle at the applicable residual value, subject to the final agreement.</p><button className="btn btn-primary">Explore buy-out</button></div></div>}{active==='Charging'&&<div className="grid action-grid"><div className="card form-card"><div className="eyebrow">Charging credits</div><h2>Available balance</h2><div className="price">₹—</div><p className="muted">Exact credit/token mechanics to be configured.</p></div><div className="card form-card"><div className="eyebrow">Recent charging</div><h2>Activity</h2><p className="muted">Your latest charging sessions will appear here.</p><button className="btn btn-primary">Find a Charger</button></div></div>}{!['Dashboard','End of Subscription','Charging'].includes(active)&&<div className="card form-card"><h2>{active}</h2><p className="muted">This module is structured and ready for the corresponding customer data and workflow.</p></div>}</main></div></>}
